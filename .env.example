# Copy to .env and adjust. Only non-secret settings live here for now (Milestone 1 has no
# collector/alerting config yet -- those land in later milestones).

NODE_ENV=production
PORT=3001
HOST=0.0.0.0
DATABASE_PATH=/data/kangdocker.sqlite
LOG_LEVEL=info
